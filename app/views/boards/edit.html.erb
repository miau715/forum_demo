<h1>Editing board</h1>

<%= form_for @board, :url => board_path(@board) do |f| %>
<%= render :partial => 'form', :locals => { :f => f } %>
<% end %>
<%= link_to 'Show', board_path(@board) %> |
<%= link_to 'Back', boards_path %>